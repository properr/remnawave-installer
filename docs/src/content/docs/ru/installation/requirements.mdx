---
title: Обязательные требования
description: Подготовка к установке компонентов Remnawave
---

import { Steps } from '@astrojs/starlight/components';

## Поддержка версий

| Remnawave | Скрипт   |
|-----------|----------|
| 2.0.0+    | 2.0.0+   |

## Remnawave Panel

### Системные требования

- **OS**: Рекомендуется **Ubuntu** или **Debian**.
- **RAM**: Минимум 2GB, рекомендуется 4GB.
- **CPU**: Минимум 2 ядра, рекомендуется 4 ядра.
- **Storage**: Минимум и рекомендуется 20GB.

## Remnawave Node

### Системные требования

- **OS**: Рекомендуется **Ubuntu** или **Debian**.
- **RAM**: Минимум 1GB.
- **CPU**: Минимум 1 ядро.

:::tip
`Remnawave` может работать с меньшими системными требованиями, но это не рекомендуется. Некоторые фоновые процессы могут быть ресурсоемкими.

`Remnawave Node` сам по себе не требует много ресурсов, но Xray-core может потреблять много CPU и RAM под нагрузкой.
:::

## Настройка доменов

Скрипт поддерживает два метода получения SSL-сертификатов:
- **Cloudflare**: Управление через API Cloudflare
- **ACME**: Прямая интеграция с хостинг-провайдером

### Примеры конфигурации DNS

#### Установка на одном сервере (панель + нода вместе)

| Тип записи | Имя               | Значение         | Статус прокси |
|------------|-------------------|------------------|---------------|
| A          | example.com       | your_server_ip   | DNS only      |
| CNAME      | panel.example.com | example.com      | DNS only      |
| CNAME      | sub.example.com   | example.com      | DNS only      |
| CNAME      | node.example.com  | example.com      | DNS only      |

:::tip
Запись `node.example.com` является необязательной для функциональности SelfSteal. Вы можете использовать корневой домен `example.com` вместо нее.
:::

#### Распределенная установка (панель и нода на разных серверах)

| Тип записи | Имя               | Значение                | Статус прокси |
|------------|-------------------|-------------------------|---------------|
| A          | example.com       | panel_server_ip         | DNS only      |
| CNAME      | panel.example.com | example.com             | DNS only      |
| CNAME      | sub.example.com   | example.com             | DNS only      |
| A          | node.example.com  | node_server_ip          | DNS only      |